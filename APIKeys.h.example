#pragma once

namespace APIKeys
{
    // Replace this with your actual AcoustID API key
    static constexpr const char* AcoustIdApiKey = "YOUR_API_KEY_HERE";

    // Telemetry upload (Google Apps Script Web App). Paste your deployed
    // /exec URL here to enable uploads; leave empty to keep data local only.
    static constexpr const char* TelemetryEndpointUrl = "";
    // Shared secret sent with each upload; must match SECRET in the Apps Script.
    static constexpr const char* TelemetrySharedSecret = "";
}
